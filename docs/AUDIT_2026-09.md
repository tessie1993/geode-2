# Repository audit, September 2026: bugs, incomplete features, fix plan

## Context

The user asked for a read-only scan of `tessie1993/geode-app` (Android music player + GPU
visualizer; Kotlin/Compose in `app/` and `engine/`, C++20 core in `core/`, JNI in
`app/src/main/cpp`, shaders in `app/src/main/assets/shaders`) to list every bug and every
incomplete feature, then a plan that small Sonnet agents can execute without conflicting with
each other or the codebase's conventions, and finally to offload that plan to a fleet of Sonnet
agents.

Constraints the user set:
- No building, no unit tests, no device steps (the Android SDK/NDK is not installed here).
- Workers verify by running the read-only `code-review` skill on their diff and fixing findings.
- One draft PR per unit, each on its own branch `claude/repo-audit-fix-plan-rfw176-uNN`.
- No assumptions, no hallucination: every item below was located in the tree and quoted by
  file:line by the scan agents; items the code contradicted were dropped (listed at the end).

Three scans ran: docs/CHANGELOG/comments for self-admitted gaps; Kotlin app + engine for bugs
and unfinished wiring; native core + JNI + shaders + tools. GitHub has zero open issues. Three
open draft PRs (#4, #7, #14) sit on other branches and are left alone.

There are no literal `TODO`/`FIXME` markers outside `third_party/`; every gap lives in prose or
in code that is written but unreachable.

## Findings

### Bugs (confirmed in code)

Kotlin / engine
- `PresetStore.fromJson` defaults for `symmetry` (0), `fluidCatchPoints` (0), `fluidParticleLife`
  (12.0) disagree with `SceneParams` (6, 2, 6f) — `app/.../data/PresetStore.kt:358,442,445` vs
  `engine/scenes/.../render/scene/SceneParams.kt:35,102,106`. Old presets/links decode wrong.
- Transition style/id/duration never persisted and never call `vizStateStore.persist()` —
  `app/.../ui/VisualSettingsController.kt:84-99`; `VizStateStore` only stores live_state + milk_path.
- `sectionStaging` never persisted; `randomEnabled` not stored though its sibling
  `vizPlaylistEnabled` is — `app/.../ui/AutoVisualsController.kt:195-204,112-123`,
  `app/.../ui/AutoVisualsPrefsStore.kt`.
- `GuiPrefs.micReactive` written on every mic toggle but never saved or loaded —
  `app/.../ui/AppTheme.kt:147`, `app/.../ui/PlayerSession.kt:119-121`.
- `NativeViz.handle` is a plain var written on GL and main threads; `create()` unsynchronised —
  `engine/scenes/.../render/bridge/NativeViz.kt:22-43`; `EnginePlumbing.kt:87-93` mutates off the GL
  thread while line 77 in the same file uses `queueEvent`.
- `VisualizerRenderer.onMilkPresetLoaded` lacks `@Volatile` while its sibling has it —
  `engine/scenes/.../render/VisualizerRenderer.kt:67-70`.
- `AnalysisEngine.close()` destroys the native analyzer while the loop may still be inside
  `analyze()` — `engine/scenes/.../analysis/AnalysisEngine.kt:198-206`.
- `OfflineAnalyzer` leaks the native `StreamingPipeline` on every non-success path (the AIFF branch
  a few lines up closes it correctly) — `engine/scenes/.../analysis/OfflineAnalyzer.kt:67,134-142`.
- `NativePlayer.handleRelease()` calls `playerDestroy` while worker tasks may still run
  `playerOpen`/`playerSetNext` — `app/.../playback/NativePlayer.kt:149-157,293-332`.
- `WidgetPublisher.clear()` cancels its scope right after launching the redraw —
  `app/.../widget/WidgetPublisher.kt:70-74`.
- `PlaybackFades`: nested `fadeThen` orphans the fade-in job; loop runs `steps+1` times —
  `app/.../ui/PlaybackFades.kt:50-62,83-86`.
- Binder IO (`Settings.Secure`, `MediaSessionManager`, `PackageManager`) on Main every 500 ms —
  `app/.../ui/PlayerSession.kt:1235-1250` → `CaptureController.kt:192-196`.
- Up to 1001 synchronous `ContentResolver.query` calls on Main when opening files —
  `app/.../ui/QueueController.kt:73-79,127-135`.
- Android Auto `onGetChildren`: `page * pageSize` Int overflow → negative `subList` index —
  `app/.../playback/PlaybackService.kt:81-84`; `LibraryTree.tracks()` rescans MediaStore per node
  and twice per playlist — `LibraryTree.kt:105-116`.
- `HistoryStore` trims by recency but ranks by play count, so most-played entries vanish after
  restart — `app/.../data/HistoryStore.kt:141`.
- `ThermalGovernor.pacedFps` read every frame, written by nobody (native governor always sees 0) —
  `engine/scenes/.../render/ThermalGovernor.kt:22`, `VisualizerRenderer.kt:199`.
- `AudioTranscoder.durationUs` pads the last AAC frame with a hardcoded 24 ms —
  `app/.../export/AudioTranscoder.kt:23-26`.
- `SmartPlaylistStore` drops the whole file on an unknown enum token —
  `app/.../data/SmartPlaylistStore.kt:158`; `EditorProjectJson.kt:225` uses `origin!!`;
  `SessionStore` writes a version it never reads — `SessionStore.kt:51,64-85`.
- `AudioCapturePump.stop()` may return with the worker still writing into the sink —
  `app/.../audio/AudioCapturePump.kt:102-109`; `PlaybackCaptureService` posts its notification
  before honouring `ACTION_STOP` — `PlaybackCaptureService.kt:38-42`; `PlaybackService.ensureRunning`
  uses `startService` and swallows the background `IllegalStateException` — `PlaybackService.kt:212-216`.
- `NativePlayer.handleAddMediaItems` adjusts `currentIndex` only when something is loaded, unlike
  remove — `NativePlayer.kt:220`; `RenderEta.latestMs` written, never read — `RenderEta.kt`.
- "Loop safe" chip silently does nothing without a tempo — `app/.../ui/SettingsDialog.kt:231-234`.

Native
- Resampler: unsigned underflow / out-of-range `erase` when input rate > 4× output —
  `core/audio/player/Resampler.cpp:37,56-66`.
- `AnalysisSession::pull`: `buffered_ - hop` underflows when hop > fftSize (≥122.88 kHz sources
  with the 60 Hz offline hop) — `core/analysis/AnalysisSession.cpp:75,85-89`.
- Waveform block average divides by `fftSize/128 == 0` for small FFT sizes → NaN into shaders —
  `AnalysisSession.cpp:133-140`; `geode_api.cpp:29` only requires `fft_size >= 2`.
- Transition progress is NaN at 0 ms duration so the outgoing scene is never released; and
  `transitionDurationMs_`, `requestedParams_`, `layerMix_`, `layerBlend_`, `transitionId_`,
  `reducedMotion_` are read on the GL thread without `stateLock_` — `core/viz/RendererFrame.cpp:167,
  229-233,94-95`, `Renderer.cpp:328`.
- `deliverPcm` holds `stateLock_` across the scene callback, so the PCM producer can block —
  `RendererFrame.cpp:145-148` (contrast `feedFormDrive` at 109-119).
- `geode_player_set_dsp` can return on its 200 ms timeout with the audio thread still inside the
  old chain — `core/audio/player/Mixer.cpp:49-57`.
- Limiter rescans the whole lookahead window per sample (O(n) in the audio callback) —
  `core/audio/dsp/Limiter.cpp:29-32`; no `setSampleRate` on any DSP stage, so a route change to a
  96 kHz DAC leaves a 48 kHz-designed EQ and half-length limiter — `DspChain.hpp:14-16`,
  `Player.cpp:233-247`.
- JNI `drumsStep` never checks the `bands` array length against the handle's band count —
  `app/src/main/cpp/geode_jni.cpp:198-204`, `core/analysis/DrumChannels.cpp:22`.
- `ReactiveAnalyzer::reset()` does not clear `lastFrameSilent_` — `ReactiveAnalyzer.cpp:113-157`;
  `AnalysisSession::push` grows its buffer without bound — `AnalysisSession.cpp:58-62`.
- `Output::isOpen()` is true for a stream Oboe already closed; `Player::run` handles commands
  before the disconnect check — `Output.hpp:25`, `Output.cpp:83-86`, `Player.cpp:107-112`;
  `postCommand` can spin forever — `Player.cpp:320-329`.
- Tags: NULL and "" both overwrite a field although the header says NULL clears and the JNI never
  passes NULL, so a short array blanks the rest — `geode_tags_api.cpp:66-78`, `Tags.cpp:89-95`,
  `geode_tags_jni.cpp:70-73`; `writeTags` returns bare false for four causes — `Tags.cpp:82-103`.
- GL teardown asymmetry on surface recreation (`zeroTex_`, `quadVao_` never deleted between
  contexts) — `Renderer.cpp:214-256`, `CompositePass.cpp:22-25`; `geode_viz_last_error` returns a
  pointer into GL-thread-mutated state — `geode_viz_api.cpp:229-231`.
- Nested `//#include` inside a shader library is silently dropped — `core/viz/ShaderSource.cpp:80-85`.
- `tools/check_style_collection.py:31-38` reads six `dev/musicviz` paths that no longer exist.

### Incomplete features (written but unreachable, or self-admitted)

- Video templates: `TemplateFormat.kt`, `VideoTemplate.kt`, `TemplateRepository.kt` (~1980 lines)
  have no caller, no DI binding, no screen, no strings; `geode://template/...` host is not in the
  manifest (`AndroidManifest.xml:126-128` registers only `preset`).
- Loop render / long-form: `export/LoopRender.kt` (858 lines), `export/LoopExtend.kt` (698) have
  no caller; `GeodeContainer.RENDER_SCRATCH_PREFIXES` sweeps `geode_loop_` files nothing creates.
- EBU R128 loudness: `export/LoudnessMeter.kt`, `export/LoudnessTarget.kt` have no caller; no
  export path normalises loudness.
- Playlist file formats: `data/PlaylistFormats.kt` (M3U/PLS/XSPF parse + resolve) has no caller.
- Tag writing for MediaStore tracks the app did not create: README maintainer note says the
  library screen does not launch `MediaStore.createWriteRequest` yet; `grep createWriteRequest`
  finds nothing; `DuplicatesTab.kt:44-85` already does the sibling `createDeleteRequest` flow.
- Analysis cache (format v2) does not store the musical-structure fields (`tempoStability`,
  `barPhase`, `beatInBar`, `downbeat`, `downbeatConfidence`, `novelty`, `sectionBoundary`), so a
  timeline loaded from cache has zeros there — `CHANGELOG.md:54-56`, `AnalysisCache.kt:14,140+`.
- Kotlin `LfoEngine.tick` / `AdsrEngine.tick` have no callers; `VisualizerRenderer.syncNativeState`
  forwards only `.configs` to native — `Lfo.kt:185`, `Adsr.kt:41`, `VisualizerRenderer.kt:75-77,185-194`.
- Unused strings that describe UI nobody draws: `studio_reset`, `studio_nothing_changed`,
  `studio_renders_new_file`, `action_visualise`, `action_export_now`, `action_add_to_studio`,
  `settings_tab_help`, `settings_intent_body` — `strings.xml:50,80,287-289,381-383`;
  `AppSettingsTab.kt:24` hardcodes the tab titles instead.
- `docs/AUDIO_CHAIN.md:96-110` specifies a "DSP does not move the visuals" sentence on every
  audio-DSP screen; not present in any string or composable.
- `docs/PARAM_MATRIX.md` lacks the live `marchDetail` row, names `FxCompositor` (absent) and the
  old Kotlin scene classes, and marks 20+ family cells as direct readers where the native family
  source never reads the param (list in the docs unit).
- `ENABLE_PLAYLIST ON` packages `libprojectM-4-playlist.so` that nothing calls (README says set OFF).
- `tools/shaderpreview` is documented as stale and unable to run since the C++ port.
- Doc mismatches: `android.yml:73-74` cites a `ShaderSyntaxTest` that does not exist;
  `release.yml:44` comment contradicts line 49; `AUDIO_FEATURE_ABI.md:21` links a missing
  `ENGINE_V2_PLAN.md`; `SOURCE_ARCHIVE.md:152-156` describes a `checkThirdPartyNotices` task that
  does not exist (the check lives in `geode.provenance.gradle.kts`); `provenance.json` and
  `reference-coverage.json` notes cite `MASTER_PLAN.md`, `EngineProvenanceRegistryTest`,
  `ReferenceCoverageTest` that are absent; `PARAM_MATRIX.md:9-11` says a test enforces the columns.

## Work units (24; one branch and one draft PR each, branches `claude/repo-audit-fix-plan-rfw176-uNN`)

Rules that apply to every unit:
- Branch: `claude/repo-audit-fix-plan-rfw176-uNN` from the current HEAD of
  `claude/repo-audit-fix-plan-rfw176`. Draft PR against `main`.
- Touch only the files listed. Files marked **shared** are edited by more than one unit: add new
  members at the end of the relevant class/section, never reorder or reformat existing code, and
  keep the hunk as small as possible so the PRs merge cleanly.
- New strings go in `app/src/main/res/values/strings.xml` immediately after the existing block the
  unit names (not at the end of the file).
- Do not edit `CHANGELOG.md`, any `detekt-baseline.xml`, or `README.md` unless the unit says so.
  The coordinator writes the changelog entry after merge.
- No ABI change to existing `core/api/geode_api.h` functions; additions only, appended at the end
  of the matching section, with the JNI in the matching `app/src/main/cpp/*.cpp` and the
  `external fun` appended at the end of `GeodeNative.kt` (**shared**).

| # | Title | Files | Change |
|---|---|---|---|
| U01 | Serialization round-trips | `app/.../data/PresetStore.kt`, `SmartPlaylistStore.kt`, `EditorProjectJson.kt`, `SessionStore.kt` | Use `SceneParams.DEFAULT.symmetry/fluidCatchPoints/fluidParticleLife` as the `fromJson` fallbacks; parse `RuleField`/`RuleOp` with `entries.firstOrNull` and skip only the bad rule; hoist `origin ?: return MarkerOrigin.Manual`; read `KEY_VERSION` in `SessionStore.parse` and quarantine newer files via `AtomicWrite.quarantine`. |
| U02 | Persist transition and auto-visual toggles | `app/.../ui/VisualSettingsController.kt`, `ui/VizStateStore.kt`, `ui/AutoVisualsController.kt`, `ui/AutoVisualsPrefsStore.kt` | Add `KEY_TRANSITION_ID`/`KEY_TRANSITION_DURATION` to `VizStateStore` (same shape as `KEY_MILK_PATH`), restore them in the initial state, call `persist()` from the three transition setters; add `KEY_SECTION_STAGING` and `KEY_RANDOM_ENABLED` to `AutoVisualsPrefsStore.save/applyTo` and call `persistAutoVisuals()` from `setSectionStaging`. |
| U03 | Capture path: mic persistence and main-thread binder IO | `app/.../ui/AppTheme.kt`, `ui/CaptureController.kt`, `ui/PlayerSession.kt` (**shared**, only the `pollPlaybackState` block ~1235-1250), `audio/AudioCapturePump.kt`, `audio/PlaybackCaptureService.kt` | Save/load `micReactive` in `ThemeStore.saveGui/loadGui` next to `KEY_KEY_COLOR` and restore mic state on session start; run `refreshExternalAudio()` on `storeScope`/IO and only when `_externalAudio.value.active || awaitingConsent`; make the pump worker check `runGeneration` before each `sink.write`; check `ACTION_STOP` before `startForegroundNotification()`. |
| U04 | Small playback/export correctness | `app/.../ui/PlaybackFades.kt`, `data/HistoryStore.kt`, `export/AudioTranscoder.kt`, `export/RenderEta.kt` | Capture the fade job locally and null the field only `if (fadeJob === self)` (pattern: `SleepTimer.kt:57`); loop `1..steps` after setting the start volume; persist the union of the 200 most recent and `mostPlayed(200)`; derive AAC frame duration as `1_024_000_000L / sampleRate` from the output format; remove `RenderEta.latestMs` or read it where the ETA is shown. |
| U05 | Native player release and widget clear | `app/.../playback/NativePlayer.kt` (**shared**), `widget/WidgetPublisher.kt` | `shutdownNow()` + bounded `awaitTermination`, and `if (released) return` at the top of the `openCurrent`/`queueNext` worker bodies; drop the `loadedId >= 0` clause so insert adjusts `currentIndex` like remove; run the widget's final `updateAll` on `GeodeContainer.appScope` before cancelling the publisher scope. |
| U06 | Android Auto browse | `app/.../playback/PlaybackService.kt`, `playback/LibraryTree.kt` | Compute page bounds in `Long` and coerce into `0..all.size`; memoise `tracks()` per `LibraryTree` instance with invalidation on `onGetLibraryRoot`; use `ContextCompat.startForegroundService` in `ensureRunning` (or document why not, in the manifest's existing comment style). |
| U07 | Engine thread safety | `engine/scenes/.../render/bridge/NativeViz.kt`, `app/.../ui/EnginePlumbing.kt`, `engine/scenes/.../render/VisualizerRenderer.kt` (**shared**, only lines 67-70), `engine/scenes/.../analysis/AnalysisEngine.kt` | `@Volatile handle`, `@Synchronized create()`; route the mutating calls at `EnginePlumbing.kt:87-93` through `visualizerView.queueEvent { }` exactly as line 77 does, after checking `geode_api.h`'s any-thread/GL-thread split for each call; add `@Volatile` to `onMilkPresetLoaded`; make `close()` cancel-and-join (or a `@Volatile closed` guard in the loop) before `analyzer.close()`, and synchronise `job` access. |
| U08 | Frame pacer publishes its cap | `engine/scenes/.../render/ThermalGovernor.kt`, `render/FramePacer.kt`, `render/VisualizerView.kt`, `app/.../wallpaper/VisualizerWallpaperService.kt` | Set `ThermalGovernor.pacedFps` whenever a `FramePacer` starts or its policy changes (Capped(n) → n, otherwise 0), from both the view and the wallpaper; keep the ThermalGovernor doc comment accurate. |
| U09 | Queue open off the main thread | `app/.../ui/QueueController.kt` | Build the queue with placeholder titles, then resolve `DISPLAY_NAME` on `PlayerSession.storeScope` and update via `player.replaceMediaItem`; follow how `MusicLibraryController.openableInfoFor` already does the query off-thread. |
| U10 | Offline analysis: leak and cache v3 | `engine/scenes/.../analysis/OfflineAnalyzer.kt`, `analysis/AnalysisCache.kt` | Close the `StreamingPipeline` in the same `finally` as the codec/extractor (mirror the AIFF branch at 60-63); bump `AnalysisCache.VERSION` to 3 and serialise/deserialise the seven structure fields of `AudioFeatures` (v2 files are simply treated as a miss, as the existing version check already does). |
| U11 | Wire the video-template subsystem | read `data/TemplateFormat.kt`, `data/VideoTemplate.kt`, `data/TemplateRepository.kt`; new `ui/TemplateController.kt`, new `ui/TemplatesSheet.kt`; edit `ui/PlayerSession.kt` (**shared**, append a controller + facade methods), `ui/VisualsHub.kt` (**shared**, one entry point next to the presets section), `AndroidManifest.xml` (add `<data android:host="template"/>` to the VIEW filter), `GeodeContainer.kt`/`di/DataModule.kt` if a binding is needed, `strings.xml` after the `preset_*` block | Minimum: list saved templates, save the current look as a template, apply a template, import from a `geode://template/...` link and from a file, share as link. Follow the `*Controller` + nested `interface Host` shape and the `PresetLibraryController` as the reference; no back-reference to `PlayerSession`. |
| U12 | Wire loop render / long-form export | read `export/LoopRender.kt`, `export/LoopExtend.kt`; edit `ui/ExportController.kt` (**shared**, append), new `ui/LoopRenderSheet.kt`, `ui/ExportHost.kt` (**shared**, one entry point), `strings.xml` after the `export_*` block | Add `startLoopRender(spec)` / `cancelLoopRender()` on `ExportController` in the same shape as `startStudioExport` (same `ExportRun.scope`, same progress/result state pattern), and a sheet to choose loop length/drift and start it. Keep `GeodeContainer.RENDER_SCRATCH_PREFIXES` in step. |
| U13 | Loudness target in export | read `export/LoudnessMeter.kt`, `export/LoudnessTarget.kt`; edit `ui/SettingsDialog.kt`, `export/VideoExporter.kt`, `ui/ExportController.kt` (**shared**, append), `strings.xml` after the `export_*` block | Add a loudness-target chip row (targets from `LoudnessTargets`) persisted with the other export defaults, measure with `LoudnessAnalyser` before encoding and apply the advised gain in `VideoExporter.encodeInto`; also pass `enabled = barUs != null` to the "Loop safe" chip. |
| U14 | Playlist file import | read `data/PlaylistFormats.kt`; edit `ui/MusicLibraryController.kt` (**shared**, append `importPlaylistFile`), the playlists screen in `ui/` (find the composable that lists playlists; add an "Import playlist…" action with an `OpenDocument` launcher), `strings.xml` after the `playlist_*` block | Parse with `PlaylistFormats.parse`, resolve with `resolve` against the library, create the playlist through the existing create path, report unresolved entries via the existing snackbar/result mechanism. If `PlaylistFormats` has a writer, add the matching export action. |
| U15 | Tag-write consent flow | `ui/TrackInfoEditor.kt`, `ui/MusicLibraryController.kt` (**shared**, `writeTrackInfo` only), `data/NativeTags.kt`, `ui/PlayerSession.kt` and `ui/LibraryViewModel.kt` (**shared**, the `writeTrackInfo` passthroughs only), `README.md` lines 107-109 only | Make `NativeTags.write` report a sealed outcome (`Written`, `NeedsConsent`, `Refused`, `Unsupported`) with `SecurityException`/`RecoverableSecurityException` → `NeedsConsent`; on `NeedsConsent` the editor launches `MediaStore.createWriteRequest` through `StartIntentSenderForResult` exactly as `DuplicatesTab.kt:44-85` does for delete, then retries; update the README maintainer note. |
| U16 | UI completions from orphaned strings and docs | `ui/AppSettingsTab.kt`, `ui/AudioSettings.kt`, the Studio clip-edit sheet under `ui/studio/` (find it), `strings.xml` (edit existing entries only) | Replace the hardcoded `SETTINGS_TAB_TITLES` with string resources (use `settings_tab_help`); add the "everything the user can do to the sound is inaudible to the picture" sentence from `docs/AUDIO_CHAIN.md:96-110` to each audio-DSP screen; add a Reset action to the clip-edit sheet using `studio_reset`/`studio_nothing_changed`/`studio_renders_new_file`; for `action_visualise`/`action_export_now`/`action_add_to_studio`/`settings_intent_body` either wire them where a track context menu already exists or delete the strings. |
| U17 | Remove the dead Kotlin LFO/ADSR tick paths | `engine/scenes/.../render/Lfo.kt`, `render/Adsr.kt`, `render/VisualizerRenderer.kt` (**shared**, the `LfoEngine`/`AdsrEngine` fields and `syncNativeState` only) | First confirm in `core/viz` (FormDrive/modulation) and `CHANGELOG.md` that native applies the LFO/ADSR configs. If so, reduce the two classes to config holders and delete the tick/phase/follower state; if not, wire `tick` into `onDrawFrame` instead. State which in the PR. |
| U18 | Native analysis robustness | `core/analysis/AnalysisSession.cpp`, `core/api/geode_api.cpp`, `core/analysis/ReactiveAnalyzer.cpp`/`.hpp`, `core/analysis/DrumChannels.cpp`/`.hpp`, `app/src/main/cpp/geode_jni.cpp` (**shared**), `engine/audio-core/.../DrumChannels.kt` | Clamp `hop` to `buffered_` and reject `hop > fftSize` in create/setSampleRate; require `fft_size >= GEODE_WAVEFORM_POINTS` (or `max(step,1)`); cap `push` growth and drop oldest; set `lastFrameSilent_` in `reset()`; give `DrumChannels` an in-place `setSampleRateHz`; clamp the JNI `bands` length like `analysisPush` does at 79-81 and check the size in the Kotlin wrapper. |
| U19 | Native player: resampler and output lifecycle | `core/audio/player/Resampler.cpp`/`.hpp`, `Mixer.cpp`/`.hpp`, `Player.cpp`, `Output.cpp`/`.hpp` | Clamp `head_` in `compact()` and recompute `phase_`; make `setDsp` hand the old chain to the existing `retired_` ring (or otherwise guarantee the audio thread has let go) without changing the C signature; reset `stream_` in `onErrorAfterClose` (or make `isOpen()` test `!disconnected_`) and move the disconnect check ahead of the command loop; bound `postCommand` and surface failure via `noteError`. |
| U20 | Native DSP: sample-rate change, limiter cost, denormals | `core/audio/dsp/*`, `core/api/geode_dsp_api.cpp`, `core/api/geode_api.h` (append `geode_dsp_set_sample_rate` to the DSP section), the JNI file holding the dsp functions (**shared**, append), `engine/audio-core/.../GeodeNative.kt` (**shared**, append), the Kotlin DSP wrapper in `engine/audio-core`, `app/.../playback/NativePlayer.kt` (**shared**, the periodic poll only) | Add `DspChain::setSampleRate` re-running `Equalizer::recompute`, crossfeed/gain coefficients and resizing the limiter buffers; from the player's poll compare `playerOutputSampleRate` with the chain's rate and rebuild/hand over via `set_dsp` when it changes; replace the limiter window scan with a running maximum; epsilon-snap `Gain`; set FTZ/DAZ (or the documented equivalent) once when the chain is created and note it beside the "no allocation, no locks, no logging" comment. |
| U21 | Renderer thread safety and GL teardown | `core/viz/Renderer.cpp`/`.hpp`, `RendererFrame.cpp`, `CompositePass.cpp`/`.hpp`, `core/api/geode_viz_api.cpp`, `core/viz/ShaderSource.cpp` | Clamp transition duration to ≥1 ms in `setTransition`; latch `transitionId_`, `transitionDurationMs_`, `layerMix_`, `layerBlend_`, `reducedMotion_`, `requestedParams_` into frame-locals in `beginFrame` as `features_` already is; copy PCM under the lock and call `acceptPcm` outside it (as `feedFormDrive` does); delete `zeroTex_`/`quadVao_` alongside `paletteLutTex_` on surface recreation; take `stateLock_` in `Renderer::fail` and have `geode_viz_last_error` return from a copy; make `resolveIncludes` recurse with a visited set. |
| U22 | Native tags: NULL-clears semantics and failure reason | `core/api/geode_tags_api.cpp`, `core/library/Tags.cpp`/`.hpp`, `app/src/main/cpp/geode_tags_jni.cpp`, `core/api/geode_api.h` (append `geode_tags_last_error` in the tags section), `engine/audio-core/.../GeodeNative.kt` (**shared**, append) | Treat `nullptr` as "unchanged" via `std::optional` in `TrackTagEdit`; pass `nullptr` for absent JNI slots; add a last-error accessor mirroring `geode_player_last_error`'s `(char* out, size_t cap)` shape with an enum for open-failed / read-only / unparseable / save-refused. Do not change `NativeTags.kt` (U15 owns it). |
| U23 | Tools and build hygiene | `tools/check_style_collection.py`, root `CMakeLists.txt` (line 62), delete `tools/shaderpreview/`, `tools/glslcheck/README.md` (its shaderpreview mention), `.github/workflows/android.yml` (lines 73-74 comment and the step it describes), `.github/workflows/release.yml` (line 44 comment), `README.md` lines 110-112 and the `tools/` mentions only | Repoint the six stale paths to `engine/scenes/.../VisualStyleCatalog.kt`, `app/src/main/assets/shaders/cymatics_field_frag.glsl`, `core/viz/scenes/CymaticsScene.cpp` and the `dev/geode/ui/` files; set `ENABLE_PLAYLIST OFF` after grepping build files and `THIRD_PARTY_NOTICES` for `playlist` and adjusting any reference; remove the stale harness and its README references; fix the two workflow comments (remove or reword the `ShaderSyntaxTest` claim; do not change what the steps install). |
| U24 | Documentation truth pass | `docs/PARAM_MATRIX.md`, `docs/visualizer-v2/AUDIO_FEATURE_ABI.md`, `docs/visualizer-v2/SOURCE_ARCHIVE.md`, `docs/visualizer-v2/provenance.json` and `reference-coverage.json` (`note` fields only), `README.md` "Tests" and "Documentation" sections only | Add the `marchDetail` row (Shader family, `MARCHED_SCENES` scope); replace `FxCompositor`/Kotlin class names with `core/viz/scenes/<X>Scene.cpp` and `CompositePass.cpp`; re-derive the family columns with `grep -w` over `core/viz/scenes/` and move the non-reader cells to the composite column; drop the "test enforces" sentences; remove the dead `ENGINE_V2_PLAN.md` link; verify the `stereoWidth` claim at `AUDIO_FEATURE_ABI.md:62-64` against `OfflineAnalyzer.kt:178` and `AnalysisSession.cpp:124-127` and correct it; fix the `checkThirdPartyNotices` known-gap to name `checkEngineProvenance` in `geode.provenance.gradle.kts`; strip the references to absent tests/plans from the two JSON notes. |

Ownership check: no file is edited by two units except those marked **shared**, and for those each
unit is confined to a named region or to appending.

## Deferred (not in this batch, with the reason)

- Measured global flash limiter, saturated-red analysis, safety telemetry, thermal half
  (`docs/visualizer-v2/SAFETY_MODEL.md`): needs GPU readback design and device measurement.
- Sample-locked live analysis hops (`AUDIO_FEATURE_ABI.md:17-21`): V2 architecture change.
- Multi-lane project export (`export/ProjectComposition.kt:26-28`): Media3 compositor design
  change that cannot be validated headless.
- Lost `docs/DEVICE_CHECKS.md` items and the 30 `DEFER` reference effects: not recoverable from
  code / product decisions.
- `compileSdk 37` vs CI `platforms;android-36`: README records it as a deliberate choice.
- CI running detekt only on `:app` (`android.yml:84`): a CI change we cannot exercise here.
- Hardcoded Kotlin UI strings across export/ui (pattern, English-only app): separate pass, wide
  blast radius across files other units own.
- `FramePacer.stats()` unread, `FeatureRing` test-only, remaining main-thread prefs reads in
  `AppShell.kt:123`/`ExportController.kt:335`/`PlaybackEngine.kt:71`: smells, not defects.
- Dropped as contradicted by code: "bit-perfect path missing" (implemented in
  `playback/BitPerfectOutput.kt` via Android 14 mixer attributes); "offline analyzer downmixes to
  mono" (`OfflineAnalyzer.kt:178` pushes `channels`; U24 corrects the doc).

